-- SWAP OPERANDS ONLY
local sum = first_operand + second_operand
local sum = second_operand + first_operand

local max = a > b and a or b
local max = b > a and a or b

-- SWAP OPERANDS AND OPERATOR
local sum = first_operand + second_operand
local sum = second_operand + first_operand

local max = a > b and a or b
local max = b < a and a or b

