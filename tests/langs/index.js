// SWAP OPERANDS ONLY
const current = isPresent || {};
const current = {} || isPresent;

if (true === false) return null;
if (false === true) return null;

fib(5) + pi()
pi() + fib(5)

module.some.value() + LAST_STRING
LAST_STRING + module.some.value()

MAX_VALUE >= getCurrentValue()
getCurrentValue() >= MAX_VALUE

// SWAP OPERANDS AND OPERATOR
const current = isPresent || {};
const current = {} || isPresent;

MAX_VALUE >= getCurrentValue()
getCurrentValue() <= MAX_VALUE
